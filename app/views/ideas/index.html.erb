<%= render 'carousel' %>

<div class="container">
  <div class="row">
    <% @ideas.each do |idea| %>
      <div class="col-md-4">
        <div class="card mb-4 shadow-sm">

          <h5 class="card-header">
            <%= link_to  idea.name, idea_path(idea), class: "btn btn-outline-dark" %>

            <div class="btn-group">
              <% if can?(:update, Idea) %>
                <%= link_to  "Edit", edit_idea_path(idea), class: "btn btn-outline-warning"  %>
              <% end %>

              <% if can?(:destroy, Idea) %>
                <%= link_to  "Delete", idea_path(idea), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-outline-danger"  %>
              <% end %>

            </div>
          </h5>

          <div class="card-body">

            <p class="card-text">
              <%= idea.description %>
            </p>

            <div class="text-muted">
              <%= idea.likes.count %> likes
              <%= idea.dislikes.count %> dislikes
            </div>

            <div class="text-muted">
              <%= idea.views.count %> reviewed
            </div>
          </div>

        </div>
      </div>
    <% end %>
  </div>
</div>
